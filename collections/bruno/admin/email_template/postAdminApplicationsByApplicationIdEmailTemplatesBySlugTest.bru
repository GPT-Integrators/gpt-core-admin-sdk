meta {
  name: Create test
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/applications/{application_id}/email-templates/{slug}/test
  body: json
  auth: inherit
}

body:json {
  {
    "data": {
      "email": "string"
    }
  }
}

docs {
  Send a test email using this template
}
