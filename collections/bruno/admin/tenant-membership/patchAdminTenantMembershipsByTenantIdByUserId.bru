meta {
  name: Update tenant memberships
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/admin/tenant-memberships/{tenant_id}/{user_id}
  body: json
  auth: inherit
}

query {
  ~include: 
  ~fields: 
}

body:json {
  {
    "data": {
      "attributes": {
        "permissions": null,
        "role": null,
        "role_id": null
      },
      "id": "string",
      "relationships": {},
      "type": null
    }
  }
}

docs {
  Updates specific fields of an existing resource.

**Authentication:** Required - Admin API key
**Rate Limit:** 1000 requests per minute

}
