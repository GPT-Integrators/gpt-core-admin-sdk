meta {
  name: Create config enums
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/extraction/config-enums
  body: json
  auth: inherit
}

query {
  ~include: 
  ~fields: 
}

body:json {
  {
    "data": {
      "attributes": {
        "description": null,
        "is_active": null,
        "label": "string",
        "sort_order": null,
        "type": "string",
        "value": "string"
      },
      "relationships": {},
      "type": null
    }
  }
}

docs {
  Creates a new resource. Returns the created resource with generated ID.

**Authentication:** Required - Admin API key
**Rate Limit:** 1000 requests per minute

}
