meta {
  name: Update revoke
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/admin/invitations/{id}/revoke
  body: json
  auth: inherit
}

query {
  ~include: 
  ~fields: 
}

body:json {
  {
    "data": {
      "attributes": {},
      "id": "string",
      "relationships": {},
      "type": null
    }
  }
}

docs {
  Updates specific fields of an existing resource.

**Authentication:** Required - Admin API key
**Rate Limit:** 1000 requests per minute

}
