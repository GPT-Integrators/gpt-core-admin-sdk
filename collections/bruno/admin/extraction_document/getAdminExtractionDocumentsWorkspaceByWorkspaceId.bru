meta {
  name: Get workspace
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/extraction/documents/workspace/{workspace_id}
  body: none
  auth: inherit
}

query {
  ~filter: 
  ~sort: 
  ~include: 
  ~fields: 
  ~batch_id: 
}

docs {
  Retrieves a single resource by ID.

**Authentication:** Required - Admin API key
**Rate Limit:** 1000 requests per minute

}
