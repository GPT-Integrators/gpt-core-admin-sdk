meta {
  name: Get ai config
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/sys/ai-config/{id}
  body: none
  auth: inherit
}

query {
  ~include: 
  ~fields: 
}

docs {
  Retrieves a single resource by ID.

**Authentication:** Required - System admin token
**Rate Limit:** No limit (system admin)

}
