meta {
  name: List field templates
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/field-templates
  body: none
  auth: inherit
}

query {
  ~filter: 
  ~sort: 
  ~include: 
  ~fields: 
  ~category: 
  ~is_system: 
}

docs {
  Lists resources with optional filtering, sorting, and pagination.

**Authentication:** Required - Admin API key
**Rate Limit:** 1000 requests per minute

}
