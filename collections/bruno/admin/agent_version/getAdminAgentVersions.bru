meta {
  name: List agent versions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/agent-versions
  body: none
  auth: inherit
}

query {
  ~filter: 
  ~sort: 
  ~include: 
  ~fields: 
  ~agent_id: 
}

docs {
  Lists resources with optional filtering, sorting, and pagination.

**Authentication:** Required - Admin API key
**Rate Limit:** 1000 requests per minute

}
