meta {
  name: List agent version revisions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/agent-version-revisions
  body: none
  auth: inherit
}

query {
  ~filter: 
  ~sort: 
  ~page: 
  ~include: 
  ~fields: 
}

docs {
  Lists resources with optional filtering, sorting, and pagination.

**Authentication:** Required - Admin API key
**Rate Limit:** 1000 requests per minute

}
