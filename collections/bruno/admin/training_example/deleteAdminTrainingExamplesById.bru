meta {
  name: Delete training examples
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/admin/training-examples/{id}
  body: none
  auth: inherit
}

query {
  ~include: 
  ~fields: 
}

docs {
  Deletes a resource permanently. This action cannot be undone.

**Authentication:** Required - Admin API key
**Rate Limit:** 1000 requests per minute

}
